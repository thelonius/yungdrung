"""Чистые функции предметной области: статусы шагов, буксование, разбор дат.

Ни ввода-вывода, ни базы, ни настроек: всё, что здесь лежит, считается от
переданных аргументов и проверяется без стора. Слой `core` зовёт эти функции
над данными из `store`, адаптеры (`engine.py`, `api/`) зовут `core`. Обратной
зависимости быть не должно: `domain` не импортирует ничего из проекта.
"""
