"""Прикладной слой: операции, которые зовут оболочки.

`core` не знает ни про HTTP, ни про argparse. Функции принимают контекст
(`core.context.Context`: путь к стору и настройкам), обычные значения и
возвращают Pydantic-модели из `core.models`. Ошибки — исключения из
`core.errors`; переводить их в код возврата, HTTP-статус или текст для бота —
дело адаптера (`engine.py`, `api/`, `remind.py`), а не этого слоя.

Данные задач и шагов внутри по-прежнему словари той формы, что отдаёт `store`:
типизируются границы (вход и выход операций), а не самый проверенный код
вычислений в `domain/`.
"""
