"""Прикладной слой вложений: владелец (задача, шаг, шаблон) → строки таблицы
`attachments` и байты на диске (§3.5 спецификации среза 2). Владелец B3.

Заготовка шага 0. Хранение байтов и дедуп по sha256 остаются в `attachments.py`
в корне; здесь только адресация владельца, ошибки ядра и модели ответов.
"""
