# Чтение и запись frontmatter стора
pyyaml>=6.0

# Выгрузка в Excel (R8)
openpyxl>=3.1

# Календарная математика повторений (R23): RFC 5545 — bysetpos, bymonthday=-1,
# byweekday. Своими руками «второй вторник» и «последний день месяца» писать
# незачем, эта реализация отлажена годами.
python-dateutil>=2.8

# Русская морфология для автораспознавания базы знаний и поиска (R17, R24).
# 15 МБ словарей, чистый Python, работает офлайн — без сети и без torch.
pymorphy3>=2.0

# Модели ответов ядра (`core/models.py`): типизированный контракт с оболочками,
# из него же FastAPI соберёт OpenAPI (REFACTOR.md, срез 1b). Чистое колесо.
pydantic>=2.5

# HTTP-слой (`api/`): маршруты, OpenAPI из моделей, структурные ошибки с путём
# поля (REFACTOR.md, срез 1b). Чистые колёса, сервер слушает только 127.0.0.1.
fastapi>=0.115
uvicorn>=0.30

# Разбор multipart/form-data для загрузки вложений через `/api/v1` (срез 2).
# FastAPI без него отказывается поднять маршрут с `File()` прямо при старте
# приложения, то есть у заказчика упал бы весь сервер, а не одна кнопка.
# `tools/update.py` ставит зависимости при изменении этого файла. Версия
# сверена с установленной (`pip show python-multipart` → 0.0.6).
python-multipart>=0.0.6
