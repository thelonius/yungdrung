# Инструменты разработки. На ноутбук заказчика НЕ едут: там ставится только
# requirements.txt. Разделение намеренное — R5 («без сложной инфраструктуры»)
# про его машину, а не про нашу.
#
#   pip install -r requirements.txt -r dev-requirements.txt
#   make check

# Тесты движка и оболочки
pytest>=8.0

# Линтер. Конфигурация — в pyproject.toml, там же объяснены исключения
ruff>=0.6

# Проверка типов. Пока нацелена только на новые модули (core/, domain/, store/,
# api/) — см. pyproject.toml и REFACTOR.md, срез 1. На engine.py её включать
# бессмысленно: 3655 строк без аннотаций дадут отчёт, который никто не прочтёт
mypy>=1.11
