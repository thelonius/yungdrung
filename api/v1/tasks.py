"""Задачи: создание, карточка, правка, отмена, закрытие, удаление
(§1.1 спецификации среза 2). Владелец B1.

Заготовка шага 0: маршруты появятся в ветке владельца. Роутер уже назван, чтобы
интегратор подключил его в `api/app.py` одной строкой `include_router`.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/api/v1", tags=["tasks"])
