"""Вложения задач, шагов и шаблонов (§1.4 спецификации среза 2). Владелец B3.

Второй роутер без префикса и вне схемы для пользовательского пути
`/вложение/{id}` заводится здесь же (Р17).

Заготовка шага 0: маршруты появятся в ветке владельца. Роутер уже назван, чтобы
интегратор подключил его в `api/app.py` одной строкой `include_router`.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/api/v1", tags=["attachments"])
