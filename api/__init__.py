"""HTTP-слой: FastAPI поверх `core`. Единственный сервер приложения.

Два семейства маршрутов. `/api/v1/*` — типизированные, на моделях
`core.models`, из них собирается OpenAPI и типы клиента. `/api/*` без версии —
`api/legacy.py`, маршруты прежнего `server.py`, перенесённые механически ради
старых страниц в `static/`; удаляются по мере переезда страниц (REFACTOR.md,
срезы 2–3) и в схему не попадают.

Слушает только `127.0.0.1`: сервер пишет в стор и не имеет аутентификации.
"""
